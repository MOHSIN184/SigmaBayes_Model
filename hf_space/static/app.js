"use strict";const $=id=>document.getElementById(id),MAX_BYTES=10*1024*1024,MAX_RECORDS=5000,EXAMPLE=$("example-sequence").textContent.trim();
const ui={sequence:$("sequence-input"),file:$("file-input"),binary:$("binary-input"),sigma:$("sigma-input"),submit:$("submit-button"),reset:$("reset-button"),example:$("example-button"),status:$("status"),body:$("result-body"),summary:$("summary")};
function alertMessage(message,type="info"){ui.status.className=`alert ${type}`;ui.status.textContent=message}
function setLoading(active){ui.submit.disabled=active;ui.submit.classList.toggle("loading",active);ui.submit.querySelector(".button-label").textContent=active?"Predicting…":"Predict sequences"}
function resetResults(){ui.body.replaceChildren();const row=ui.body.insertRow();row.className="empty";const cell=row.insertCell();cell.colSpan=7;cell.textContent="Results will appear here after prediction.";ui.summary.hidden=true;ui.summary.replaceChildren()}
function cleanId(value,fallback){return(value.trim().split(/\s+/)[0]||fallback).slice(0,200)}
function lineParser(source){const records=[];let mode=null,current=null,index=0;return{push(raw){const line=raw.trim();if(!line)return;if(mode===null)mode=line.startsWith(">")?"fasta":"plain";if(line.startsWith(">")){if(mode!=="fasta")throw new Error("Invalid FASTA format. Headers cannot follow plain sequence data.");if(current)records.push(current);current={id:cleanId(line.slice(1),`Sequence_${++index}`),sequence:""};return}if(mode==="fasta"){if(!current)throw new Error("Invalid FASTA format. Sequence data must follow a header beginning with '>'.");current.sequence+=line}else records.push({id:`${source}_${++index}`,sequence:line})},finish(){if(current)records.push(current);return records}}}
function parseLines(lines,source){const parser=lineParser(source);for(const line of lines)parser.push(line);return parser.finish()}
async function parseFile(file){if(!file)return[];if(file.size>MAX_BYTES)throw new Error("The uploaded file exceeds the 10 MB limit.");if(!/\.(fasta|fa|txt)$/i.test(file.name))throw new Error("Upload a .fasta, .fa, or .txt file.");const reader=file.stream().getReader(),decoder=new TextDecoder(),parser=lineParser("File");let buffer="";for(;;){const{value,done}=await reader.read();buffer+=decoder.decode(value||new Uint8Array(),{stream:!done});const lines=buffer.split(/\r?\n/);buffer=lines.pop()||"";for(const line of lines)parser.push(line);if(done)break}if(buffer)parser.push(buffer);return parser.finish()}
function uniqueRecords(records){const seen=new Map();return records.map(record=>{const count=(seen.get(record.id)||0)+1;seen.set(record.id,count);return{id:count===1?record.id:`${record.id}_${count}`,sequence:record.sequence}})}
async function collectRecords(){let records=parseLines(ui.sequence.value.split(/\r?\n/),"Sequence");records=records.concat(await parseFile(ui.file.files[0]));if(!records.length)throw new Error("Please enter a DNA sequence or upload a FASTA file.");if(records.length>MAX_RECORDS)throw new Error("A maximum of 5,000 sequences is allowed per batch.");return uniqueRecords(records)}
function cell(row,value,className=""){const node=row.insertCell();node.textContent=value;if(className)node.className=className;return node}
function renderResults(data){ui.body.replaceChildren();for(const result of data.results){const row=ui.body.insertRow();cell(row,result.id);cell(row,String(result.length));const prediction=cell(row,result.prediction||"—");if(result.prediction){const badge=document.createElement("span");badge.className=`badge ${result.prediction==="Promoter"?"promoter":"non-promoter"}`;badge.textContent=result.prediction;prediction.replaceChildren(badge)}cell(row,result.confidence==null?"—":`${(result.confidence*100).toFixed(2)}%`);cell(row,result.sigma_factor||"");cell(row,`${result.processing_time_ms.toFixed(1)} ms`);cell(row,result.error||result.status,result.error?"status-error":"")}if(data.results.length===1&&data.results[0].status==="Success")renderSummary(data.results[0]);else{ui.summary.hidden=false;ui.summary.className="alert info";ui.summary.textContent=`Processed ${data.processed} sequence(s); ${data.failed} failed. Total server time: ${(data.total_time_ms/1000).toFixed(2)} s.`}}
function renderSummary(result){ui.summary.hidden=false;ui.summary.className="summary-card";ui.summary.replaceChildren();const values=[["Predicted class",result.prediction||"—"],["Confidence",result.confidence==null?"—":`${(result.confidence*100).toFixed(2)}%`],["Sequence length",`${result.length} bp`],["Processing time",`${result.processing_time_ms.toFixed(1)} ms`]];for(const[label,value]of values){const metric=document.createElement("div");metric.className="metric";const small=document.createElement("small"),strong=document.createElement("strong");small.textContent=label;strong.textContent=value;metric.append(small,strong);ui.summary.append(metric)}}
async function submit(){if(ui.submit.disabled)return;setLoading(true);alertMessage("Validating and analyzing sequences…","info");try{const records=await collectRecords();const response=await fetch("/predict-batch",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({records,run_binary:ui.binary.checked,run_sigma:ui.sigma.checked})});const data=await response.json();if(!response.ok){const detail=typeof data.detail==="string"?data.detail:(data.detail?.[0]?.msg||"Prediction request failed.");throw new Error(detail)}renderResults(data);alertMessage(data.failed?`Processed ${data.processed} sequence(s); ${data.failed} record(s) need attention.`:`Successfully processed ${data.processed} sequence(s).`,data.failed?"info":"success")}catch(error){resetResults();alertMessage(error.message||"Prediction request failed.","error")}finally{setLoading(false)}}
ui.submit.addEventListener("click",submit);ui.example.addEventListener("click",()=>{ui.sequence.value=EXAMPLE;ui.file.value="";resetResults();alertMessage("Verified 81 bp example loaded.","info");ui.sequence.focus()});ui.reset.addEventListener("click",()=>{ui.sequence.value="";ui.file.value="";ui.binary.checked=true;ui.sigma.checked=false;resetResults();alertMessage("Inputs are ready for a new analysis.","info");ui.sequence.focus()});
